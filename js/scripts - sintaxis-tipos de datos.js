// numero = 54;
    // No debemos inicializar una variable antes de declararla ya que si lo hacemos como en el ejemplo anterior nos estaria arrojando un error donde nos indica que se a iniclizado una variable antes de declararla

let numero
    numero = -5.3;
    numero = 54; 
        /* Esta es una modificacion a la variable numero, donde le asigno un nuevo valor y este reeemplaza al anterior */

let palabra = 'Hola Mundo';
    palabra = "Adios";
        /* Como podemos ver podemos utilizar comillas dobles o simples en los strings, da igual si es una u otra siempre y cuando el string lleve ambas iguales, no puede ser una y una ("Adios' - Error, asi no!) */

let respuesta = false;

const PI = 3.14;

console.log(numero, palabra);